# GV2 YOLO11 Models

The default model set lives in the GV2 firmware submodule:

```text
external/gv2-firmware/model_zoo/tflm_yolo11_od/
```

This folder contains the VST-BASE model manifest and release notes. Keep large model binaries in the submodule or use Git LFS if binaries are copied here for release packaging.
