# SD Card Layout

Current receiver output:

```text
/post.log              overwritten at boot
/frame_000001.jpg      future received image frames
```

`/post.log` contains the latest boot summary, modem time, GNSS status, memory dimensions and SD status.
