# Direct GV2 To T-SIM

This project no longer uses a XIAO broker. Grove Vision AI V2 connects directly to the T-SIM7080G-S3 via the custom PCB documented in:

```text
external/t-sim-motor-shield/
```

The firmware-side receive path belongs in:

```text
firmware/t-sim7080g-s3-base/src/
```
