# GV2 Model Flashing

Helper scripts live at the repository root:

```text
build_gv2_image.ps1
flash_gv2.ps1
```

GV2 firmware and model sources are provided through the stick-on module submodule:

```text
external/gv2-firmware/
```
